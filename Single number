class Solution
{
 public int singleNumber(int[] nums)
{
  int num=0;
  for(int i=0;i<nums.length;i++)
  {
   int c=0;
    for(int j=0;j<nums.length;j++)
     {
      if(nums[i]==nums[j])
       c++;
      }
      if(c==1)
      num=nums[i];
   }
  return num;
 } 
}

Output : It's a leetcode challenge programme and we
don't mentioned any main method here as it has stored
by default in leetcode problem itself! 
